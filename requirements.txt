# =============================================================================
# MILP-GNN-EBM benchmark - pinned dependency set for paper reproduction
# =============================================================================
# Tested with Python 3.10 / 3.11 (CPU build). For CUDA, replace torch with the
# matching wheel from https://pytorch.org/get-started/locally/ and install
# torch-geometric extras (torch-scatter) from
# https://data.pyg.org/whl/torch-<version>+<cuda>.html
# -----------------------------------------------------------------------------

# --- Scientific stack --------------------------------------------------------
numpy>=1.24,<2.2
scipy>=1.10
pandas>=2.0
pyarrow>=14.0           # parquet / HDF dispatch exports

# --- Optimisation ------------------------------------------------------------
pyomo>=6.7
highspy>=1.5            # HiGHS LP/MIP backend used by Pyomo and the LP worker

# --- Deep learning -----------------------------------------------------------
# NOTE: torch must be installed BEFORE torch-scatter because the latter
# is a source build that imports torch at build time. `pip install -r`
# already orders torch first, but if you run into "No module named 'torch'"
# during a torch-scatter build, install torch alone first:
#     pip install "torch>=2.1,<2.5"
# then install torch-scatter from the PyG wheel index (see requirements-pyg.txt):
#     pip install -r requirements-pyg.txt
torch>=2.1,<2.5
torch-geometric>=2.4
# torch-scatter is split out into requirements-pyg.txt (needs PyG wheel index)

# --- Plotting / analysis -----------------------------------------------------
matplotlib>=3.7
seaborn>=0.13

# --- Clustering / dimensionality reduction (criticality analysis) ------------
hdbscan>=0.8.33
umap-learn>=0.5.5
scikit-learn>=1.3       # transitive but pinned for determinism

# --- Configuration / I/O / progress ------------------------------------------
PyYAML>=6.0
tqdm>=4.66

# --- Optional extras (uncomment if needed) -----------------------------------
 jupyter>=1.0          # for notebooks/
 hvplot>=0.9           # richer plotting backend
 black>=23.0           # formatting (dev only)
 ruff>=0.1             # linting (dev only)
 thrml>=0.1            # for thrml
